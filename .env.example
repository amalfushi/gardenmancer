# Anthropic Claude API key for seed packet scanning and garden optimization
ANTHROPIC_API_KEY=your-api-key-here

# Set to 'true' to use mock responses instead of real Claude API
# Useful for local development and testing without an API key
MOCK_AI=true
