# 5.3 Error Handling — Plan

Add comprehensive error handling and loading states to every async operation: skeleton loading screens while data fetches, retry buttons on API failures, user-friendly error messages (no raw stack traces), toast notifications for background operations, optimistic UI updates where appropriate, and graceful degradation when features are unavailable (e.g., camera not supported, AI API down).
