# 1.2 Mantine Setup — Plan

Install Mantine core, hooks, and form packages, configure the MantineProvider in the root layout with a custom garden theme (primary color: green shades, accent: warm earth tones), set up Mantine's CSS imports, and create a theme configuration file (`src/lib/theme.ts`) that centralizes all theme customizations.
