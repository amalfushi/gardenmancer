# 1.5 App Shell — Plan

Build the responsive application shell using Mantine's AppShell component: a bottom navigation bar on mobile (Scanner, Plants, Garden icons) and a collapsible sidebar on desktop, with route-aware active states. Create placeholder pages for each section (`/scanner`, `/plants`, `/garden`) that render within the shell layout.
