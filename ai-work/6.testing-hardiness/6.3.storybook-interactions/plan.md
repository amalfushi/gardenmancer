# 6.3 Storybook Interaction Tests

Add `play()` functions to existing stories using `@storybook/test` (userEvent, expect, within). Target stories: scan result form (fill + submit), plant search (type + filter), garden form (fill + validate), add-to-garden button (open modal + select), zone selector (change zone).
