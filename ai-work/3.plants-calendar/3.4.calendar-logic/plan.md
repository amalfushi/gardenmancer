# 3.4 Calendar Logic — Plan

Implement the zone-aware planting calendar engine: given a user's USDA zone (which determines last frost date and first frost date), calculate for each plant the optimal date ranges for starting seeds indoors, transplanting outdoors, direct sowing, and expected harvest window. Handle succession planting (stagger plantings every N weeks) and store user zone preference in the lowdb settings.
