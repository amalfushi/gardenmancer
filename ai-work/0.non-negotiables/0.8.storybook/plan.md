# 0.8 Storybook

Install and configure Storybook with the Next.js framework adapter and test runner. Create a sample component with stories to verify the setup. Every UI component going forward must have an accompanying `.stories.tsx` file.
