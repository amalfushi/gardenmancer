# 0.6 Git Hooks — Plan

Install Husky and lint-staged, configure a pre-commit hook that runs ESLint (with auto-fix), Prettier (with auto-format), and Vitest (on related test files) against staged files only, ensuring broken or unformatted code can never be committed to the repository.
