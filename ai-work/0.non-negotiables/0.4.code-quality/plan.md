# 0.4 Code Quality — Plan

Configure ESLint with the Next.js recommended ruleset, TypeScript strict rules, and import ordering, plus Prettier with a shared config (single quotes, trailing commas, 100-char line width). Add `lint` and `format` scripts to `package.json` and verify they run cleanly on the initial codebase with zero warnings.
