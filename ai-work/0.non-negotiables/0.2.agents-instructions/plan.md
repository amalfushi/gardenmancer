# 0.2 Agent Instructions — Plan

Write comprehensive agent instruction files that define coding standards (TypeScript strict mode, named exports, no default exports), workflow expectations (read context.md first, update progress.md after), commit conventions (conventional commits, co-author trailers), and quality gates (tests required, coverage thresholds) so every agent operates consistently regardless of which milestone they work on.
