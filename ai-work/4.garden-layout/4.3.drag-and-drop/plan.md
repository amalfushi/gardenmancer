# 4.3 Drag and Drop — Plan

Implement drag-and-drop plant placement on the canvas: a plant palette sidebar/drawer showing queued plants (from the add-to-garden flow), drag from palette to canvas with ghost preview, snap-to-grid positioning, touch support for mobile (long-press to pick up, drag to place), and undo/redo for placement actions using a command pattern stack.
