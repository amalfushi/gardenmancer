# 4.2 Canvas Setup — Plan

Set up the react-konva Stage and Layer with a configurable grid overlay (default 1 sq ft cells with 12-inch subdivisions), zoom controls (pinch-to-zoom on mobile, scroll wheel on desktop), pan/scroll behavior, and responsive canvas sizing that fills the available viewport while maintaining the garden's aspect ratio. Include a toolbar with zoom level indicator and grid toggle.
