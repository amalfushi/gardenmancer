# 2.1 Camera/Upload UI — Plan

Build a mobile-first camera and file upload component using an HTML5 file input with the `capture="environment"` attribute for direct seed packet photography on mobile devices. Include a drag-and-drop zone for desktop users, image preview with crop/rotate controls, and file validation (max size, accepted formats: JPEG, PNG, WebP).
