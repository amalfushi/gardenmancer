# 2.4 Scan Results UI — Plan

Build the scan results review screen using Mantine form components: pre-populated editable fields for every extracted plant attribute, visual indicators showing extraction confidence (green checkmark for high confidence, yellow warning for uncertain, red X for missing), and a side-by-side view of the original seed packet image and the extracted data for easy verification.
